@page
@model GenerateRecoveryCodesModel
@{
    ViewData["Title"] = "Generar Codigos de Recuperacion de Autenticación en 2 Pasos (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<section class="content">
    <div class="container-fluid">
        <div class="row  justify-content-center">
            <div class="col-md-10 col-xl-8 col-xxl-6 col-12 mt-2">
                <partial name="_StatusMessage" for="StatusMessage" />
                <div class="card card-outline card-primary">
                    <div class="card-header">
                        <h5>@ViewData["Title"]</h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col">
                                <div class="callout callout-danger">
                                    <p class="lead">
                                        <span class="mdi mdi-alert mdi-24px text-danger"></span>
                                        <strong>Guarde estos codigos en un lugar seguro</strong>
                                    </p>
                                    <p class="lead">
                                        Si usted pierde su dispositivo y no cuenta con códigos de recuperación perderá acceso a su cuenta.
                                    </p>
                                    <p class="lead">
                                        La generación de nuevos códigos de recuperación no cambia las claves utilizadas en las aplicaciones de autenticación. Si desea cambiar la clave
                                        utilizado en una aplicación de autenticación, debe <a class="text-leader2" asp-page="./ResetAuthenticator"> restablecer sus claves de autenticación.</a>
                                    </p>
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col">
                                <form method="post" class="form-group w-100">
                                    <button class="btn btn-warning" type="submit">Generar Códigos de Recuperación</button>
                                </form>
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

